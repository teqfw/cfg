# Agent-Facing Package Documentation

The `ai/` directory contains the compact, distributable documentation for agents that consume `@teqfw/cfg`.

Read `package-api.ts` for the supported public surface and `usage.md` for integration guidance. Both documents describe the current bootstrap state; implementation-specific behavior must not be assumed until it is documented here.
